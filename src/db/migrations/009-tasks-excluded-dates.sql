-- ============================================================================
-- 009 마이그레이션: 루틴의 특정 날짜 예외 처리
-- excluded_dates: 이 루틴에서 건너뛸 양력 날짜 배열.
-- 캘린더/타임테이블에서 이 날짜의 occurrence 는 노출되지 않음.
-- ============================================================================

ALTER TABLE tasks
  ADD COLUMN IF NOT EXISTS excluded_dates DATE[] NOT NULL DEFAULT '{}';
